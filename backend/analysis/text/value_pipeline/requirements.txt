# value_pipeline 전용 의존성 (루트 requirements.txt에 추가로 필요한 것)
# 설치: pip install -r value_pipeline/requirements.txt

# ── 핵심 (필수) ─────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.5

# ── 선택 (있으면 자동으로 실데이터/고성능 경로 사용) ──────────
# Gemini 무료 티어 (에이전트 추론). GEMINI_API_KEY 필요.
langchain-google-genai>=2.0.0
# 한국 시세/상장정보 (키 불필요)
finance-datareader>=0.9.50
# 한국 재무제표 (DART_API_KEY 필요, 무료)
# 버전 미고정: 최신 0.3.x는 Python>=3.13 요구 → Python 3.12면 uv가 3.12 호환 버전 자동 선택
opendartreader
# 뉴스/종목토론 크롤링 (키 불필요)
beautifulsoup4>=4.12.0
# .env 자동 로드
python-dotenv>=1.0.0

# 참고: torch, transformers, requests 는 이 파일에 명시되지 않으며 CI에서 별도 설치됨.
#       KR-FinBERT(snunlp/KR-FinBert-SC)는 transformers+torch로 동작하며
#       미설치/미다운로드 시 사전 기반 감성으로 자동 폴백된다.
