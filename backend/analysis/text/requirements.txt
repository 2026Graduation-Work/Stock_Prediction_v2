# backend/analysis/text 블록 전체 의존성 (preprocess + value_pipeline).
# 설치: pip install -r backend/analysis/text/requirements.txt

# ── 전처리·데이터 (필수) ─────────────────────────────────────
pandas>=2.3.3
# 로컬 뉴스 엑셀(data/{종목코드}/{종목코드}_{회사명}_YYYYMMDD-YYYYMMDD.xlsx) 읽기
openpyxl>=3.1.5

# ── 파이프라인 핵심 (필수) ───────────────────────────────────
langgraph>=1.2.11
langchain-core>=1.6.3
pydantic>=2.13.5
requests>=2.32.0

# ── 감성 분석 (필수) ─────────────────────────────────────────
# 한국어 금융 감성 (KR-FinBERT, snunlp/KR-FinBert-SC).
# 미설치 시 39단어 사전으로 폴백하지만 두 백엔드는 점수 스케일이 비교 불가능하다.
# 런 간 혼용 시 조용한 regime break가 생기므로 필수로 둔다.
# (VALUE_PIPELINE_VALIDATION.md — '감성 백엔드' 기준 참조)
torch>=2.14.0
transformers>=5.17.0

# ── 재무·시세 (필수) ─────────────────────────────────────────
# 한국 재무제표 (DART_API_KEY 필요, 무료). 재무 없이는 시그널을 만들지 않는다.
# 0.2.4부터 패키지·모듈명이 소문자로 바뀌었고 Requires-Python이 >=3.13이 됐다.
# 0.2.3 소문자 휠과 0.2.4는 저장소가 휠 루트에 그대로 풀린 깨진 빌드라 import 자체가 안 되고,
# 인덱스 메타데이터와 휠 메타데이터가 어긋나 CI(3.12)의 pip가 하드 에러로 죽는다.
# 이름 변경이 정상 반영된 첫 릴리스는 0.3.0이지만 3.12에선 설치 불가 → Python 버전별로 갈라 고정한다.
# 대문자/소문자 모듈명 차이는 value_pipeline/collectors.py의 import 폴백이 흡수한다. (#135)
opendartreader>=0.3; python_version >= "3.13"
opendartreader<0.2.3; python_version < "3.13"
# 한국 시세/상장정보 (키 불필요)
finance-datareader>=0.9.202

# ── 선택 (있으면 자동으로 실데이터/고성능 경로 사용) ──────────
# Gemini 무료 티어. GEMINI_API_KEY 필요.
# 뉴스 관련성 라벨링·핵심 이벤트 추출·근거 문장 생성에만 쓴다(점수 산출 아님).
# 없으면 규칙 기반으로 동작한다.
langchain-google-genai>=4.4.0
# 뉴스 크롤링 폴백 (키 불필요)
beautifulsoup4>=4.15.0
# .env 자동 로드
python-dotenv>=1.2.3
