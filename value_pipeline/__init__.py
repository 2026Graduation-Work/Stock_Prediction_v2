"""가치투자 데이터 전처리 파이프라인 (한국 주식).

뉴스 / 소셜 / 재무제표를 수집해 멀티 에이전트(LangGraph)로 요약·분석하고,
다운스트림 주가예측 모델이 바로 쓸 수 있는 구조화 JSON 피처를 생성한다.

모든 외부 API/모델은 선택사항이며, 키나 모델이 없으면 샘플 데이터 +
규칙 기반 폴백으로 전체 파이프라인이 끝까지 동작한다.
"""

from .schema import ValueSignal

__all__ = ["ValueSignal"]
